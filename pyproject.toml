[project]
name = "corp-fin-vals-sdk"
version = "0.1.0"
description = "CorpFin Benchmark"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"

dependencies = [
  "valsai==0.3.9", # TODO: update to version with full metadata
  "model-library==0.1.11",
  "pypandoc",
  "dotenv",
]

[dependency-groups]
dev = [
  # style
  "ruff",
  "basedpyright",
]

[tool.setuptools]
py-modules = ["main"]

[tool.basedpyright]
exclude = [
  ".*",
]
reportExplicitAny = false
